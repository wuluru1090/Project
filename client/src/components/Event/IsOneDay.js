import React from 'react'

function IsOneDay(props) {
  console.log(props)
  // let startDate = date1.split('-')
  // let endDate = date2.split('-')

  // console.log(parseInt(endDate[2]) - parseInt(startDate[2]) <= 0 ? true : false)

  // return parseInt(endDate[2]) - parseInt(startDate[2]) <= 0 ? true : false
  return <></>
}

export default IsOneDay
